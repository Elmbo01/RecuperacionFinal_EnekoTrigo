import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'RecuperacionFinal_EnekoTrigo',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
